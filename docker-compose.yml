services:
  frontend:
    build: .
    container_name: frontend_app_public
    restart: always
    ports:
      - "${PORT}:5173"
    env_file:
      - .env
    environment:
      - CHOKIDAR_USEPOLLING=true
    volumes:
      - ./src:/app/src
      - /app/node_modules
    command: npm run dev -- --host 0.0.0.0

networks:
  default:
    driver: bridge
